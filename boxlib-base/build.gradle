archivesBaseName = "boxlib-base"
version = rootProject.mod_version

dependencies {
    compileOnly(project(":boxlib-client-utils"))
}

processResources {
    filesMatching('fabric.mod.json') {
        expand 'version': project.version
    }
    inputs.property "version", project.version
}

architectury {
    common("fabric", "forge")
}